{% extends 'base.html.twig' %}
{% block body %}

    <div>
        {% for message in app.flashes('notice') %}
            <div class="flash-notice">
                {{ message }}
            </div>
        {% endfor %}
    </div>
    <a href="{{ url('app_widgetorder_order') }}" class="order">Place order</a>

{% endblock %}